// Brand name shown in the tab title, login screen, sidebar and printed receipts.
// Set NEXT_PUBLIC_BRAND_NAME in the environment; this is the only place with a fallback.
export const BRAND_NAME = process.env.NEXT_PUBLIC_BRAND_NAME || 'Raja Pisang Nugget';
